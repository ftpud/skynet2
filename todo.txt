update agent.py and create ./tui/tui3.py (similar to tui2.py, so I have good view how much tokens are spen by each model, and also I want to be able to view agent execution tree - e.g. currently running agent/command and tree if agent runs other agent/command, make sure everything is visible in narrow window, window on top must show the agent work tree, windows at the bottom token usages, keep only 1 chart as a window at very buttom that should show per minute usage of all takens
